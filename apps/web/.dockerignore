# Kept out of the build context (apps/web) for the self-hosted image
# (Dockerfile, compose.yaml). Node/Vite/Cloudflare build artifacts are
# rebuilt by the image itself; nothing here is needed to produce dist/client
# or dist/server.
node_modules
dist
.wrangler
.dev.vars
.dev.vars.example
.intlayer
.vite
.tanstack
.git
.github
screenshots
__tests__
