import { expect, test } from 'bun:test'

// Placeholder so `bun test` exits cleanly on a template with no real tests
// yet. Replace with actual tests as the project grows.
test('smoke', () => {
  expect(true).toBe(true)
})
